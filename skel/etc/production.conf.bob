PROJECT_NAME = "{{{ application.name }}}"
PORT = {{{ application.port }}}
LOG = "/var/log/{{{ application.name }}}/daemon.log"
SQLALCHEMY_DATABASE_URI = "sqlite:////var/lib/{{{ application.name }}}/main.db"

SECURITY_PASSWORD_SALT = "{{{ hash_salt }}}"
SECRET_KEY = '{{{ secret }}}'

DEBUG = False
DEBUG_TOOLBAR = False
