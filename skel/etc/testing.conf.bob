PROJECT_NAME = "{{{ application.name }}}"
PORT = {{{ application.port }}}
LOG = "/var/lib/{{{ application.name }}}/testing.log"
SQLALCHEMY_DATABASE_URI = "sqlite:////var/lib/{{{ application.name }}}/testing.db"

SECURITY_PASSWORD_SALT = "{{{ hash_salt }}}"
SECRET_KEY = {{{ secret }}}

DEBUG = True
DEBUG_TOOLBAR = False
