# -*- coding: utf-8 -*-

from nose.plugins.attrib import attr
import os, shutil, tempfile, sys

sys.path.insert(0, '.')
os.environ['SETTINGS'] = "../etc/testing.conf"

from {{{ application.module }}}.Utils import TestHelpers

class {{{ application.module }}}_ViewTestCase(TestHelpers.GeneralTestCase):
    def test_login(self):
        rv = self.client.get('/login')
        assert '{{{ application.name }}}' in rv.data

    def test_index(self):
        rv = self.client.get('/', follow_redirects=True)
        assert '{{{ application.name }}}' in rv.data
