PROJECT_NAME = "{{{ application.name }}}"
PORT = {{{ application.port }}}
LOG = "/var/lib/{{{ application.name }}}/dev.log"
SQLALCHEMY_DATABASE_URI = "sqlite:////var/lib/{{{ application.name }}}/dev.db"

SECURITY_PASSWORD_SALT = "{{{ hash_salt }}}"
SECRET_KEY = {{{ secret }}}

DEBUG = True
DEBUG_TOOLBAR = True
DEBUG_TB_INTERCEPT_REDIRECTS = False
